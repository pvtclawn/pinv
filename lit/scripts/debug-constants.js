const constants = require('@lit-protocol/constants');
console.log('Keys:', Object.keys(constants));
if (constants.LIT_ABILITY) console.log('LIT_ABILITY:', constants.LIT_ABILITY);
if (constants.LitAbility) console.log('LitAbility:', constants.LitAbility);
