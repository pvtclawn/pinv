import fs from 'fs'; import satori from 'satori'; import path from 'path'; (async () => { try { const fontPath = path.join(process.cwd(), 'public/fonts/NotoEmoji-Regular.ttf'); console.log('Loading font from:', fontPath); const fontData = fs.readFileSync(fontPath); const markup = { type: 'div', props: { children: '🔥' } }; const svg = await satori(markup, { width: 100, height: 100, fonts: [{ name: 'Noto', data: fontData, weight: 400, style: 'normal' }] }); console.log('SVG Generated successfully'); if (svg.includes('path')) console.log('Contains Paths (Likely worked)'); else console.log('No Paths (Tofu?)'); } catch (e) { console.error(e); } })();
