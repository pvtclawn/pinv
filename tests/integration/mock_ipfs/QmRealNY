{
    "uiCode": "export default ({ seconds }) => <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', width: '100%', height: '100%', background: 'black', color: 'lime', fontSize: 80 }}><div>NY COUNTDOWN</div><div style={{ fontSize: 100 }}>{seconds}</div><div>SECONDS</div></div>",
    "dataCode": "const main = async () => {\n    const now = Date.now();\n    const nextNY = new Date('2027-01-01T00:00:00Z').getTime();\n    const diff = nextNY - now;\n    const seconds = Math.floor(diff / 1000);\n    return { seconds };\n};",
    "meta": {
        "title": "Real NY Counter (Seconds)",
        "description": "Live countdown powered by Lit Action"
    }
}
